---
name: Build Nightly Version

on:
  # Creates Nightly Release of main
  # Needs: secrets.PLATOMO_BUILDER_ACCESS
  push:
    branches:
      - main

jobs:
  test:
    uses: './.github/workflows/test.yml'
  update_version:
    runs-on: ubuntu-latest
    steps:
      - name: Update Version
        run: |
          cp ./OTVisiom/version.py version.py.backup
          echo "__version__ = '12.0'"> ./OTVisiom/version.py
          cat version.py.backup |tail -n+2>> ./OTVisiom/version.py
  create_nightly:
    needs: test
    uses: "platomo/GitHub-workflows/.github/workflows/reusable-create-release-with-assets.yml@main"
    secrets:
      PLATOMO_BUILDER_ACCESS: ${{ secrets.PLATOMO_BUILDER_ACCESS }}
    with:
      package-version: nightly
      draft-release: false
      pre-release: true
      delete-existing: true

